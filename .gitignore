node_modules
dist
dist-ssr
*.local
.vite
.DS_Store

# Local Netlify folder
.netlify

# Env (secrets/keys live in Netlify env vars)
.env
.env.*
!.env.example

# Page LDI autonome (générée par npm run ldi:artifact)
artifact/dist
